# Little Lemon 🍋 - Restaurant Table Booking

The table booking for the virtual **Little Lemon** restaurant` build on React.

## ⚙️ Getting Started

### Installation

Clone the repository using <mark>https://github.com/sanjusriram16/little-lemon.git</mark> and navigate to the little-lemon folder and start the development server using `npm start`. Incase of missing dependencies, run `npm install`.
